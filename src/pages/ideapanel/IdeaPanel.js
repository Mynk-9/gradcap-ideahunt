import Styles from './IdeaPanel.module.scss';

const IdeaPanel = () => {
    return (
        <div className={`page-section ${Styles.main}`}>
            <h1>{'Idea Panel'}</h1>
        </div>
    );
};

export default IdeaPanel;
